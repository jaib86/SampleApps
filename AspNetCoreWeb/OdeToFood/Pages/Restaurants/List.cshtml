@page
@model OdeToFood.Pages.Restaurants.ListModel
@{
}

<h1>Restaurants</h1>

@if (Model.Message != null)
{
    <div class="alert alert-info">@Model.Message</div>
}

<form method="get">
    <div class="form-group">
        <div class="input-group">
            <input type="search" class="form-control" asp-for="SearchTerm" />
            <button class="btn btn-sm btn-outline-primary">Search</button>
        </div>
    </div>
</form>

@*<table class="table">
    @foreach (var restaurant in Model.Restaurants)
    {
        <tr>
            <td>@restaurant.Name</td>
            <td>@restaurant.Location</td>
            <td>@restaurant.Cuisine</td>
            <td>
                <a asp-page="./Detail" asp-route-restaurantId="@restaurant.Id" class="btn btn-sm btn-outline-primary">Detail</a>
                <a asp-page="./Edit" asp-route-restaurantId="@restaurant.Id" class="btn btn-sm btn-outline-primary">Edit</a>
                <a asp-page="./Delete" asp-route-restaurantId="@restaurant.Id" class="btn btn-sm btn-outline-danger">Delete</a>
            </td>
        </tr>
    }
</table>*@

@foreach (var restaurant in Model.Restaurants)
{
    <partial name="_Summary" model="restaurant" />
}

<a asp-page="./Edit" class="btn btn-primary">Add New</a>
<br />

@Model.Message